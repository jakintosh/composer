<div id="{{.ID}}" class="modal" aria-hidden="true">
  <div class="modal__dialog" role="dialog" aria-modal="true" aria-labelledby="{{.DialogLabelID}}">
    <div class="modal__header">
      <h2 id="{{.DialogLabelID}}" class="modal__title">{{.Title}}</h2>
      <button type="button"
              class="button button--ghost button--icon modal__close"
              data-close-modal
              aria-label="{{.CloseLabelText}}">
        ×
      </button>
    </div>
    <div class="modal__body">
      {{.RenderBody}}
    </div>
  </div>
</div>
