{{if .ID}}
  <button type="button"
          class="button button--primary button--sm run-tick-button"
          data-run-id="{{.ID}}"
          data-run-display="{{.DisplayName}}"
          aria-label="Run tick for {{.DisplayName}}">
    Tick
  </button>
{{end}}
