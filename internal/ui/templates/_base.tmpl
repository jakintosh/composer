{{define "layouts/base"}}
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{block "title" .}}Composer{{end}}</title>
  <link rel="stylesheet" href="/static/app.css">
  {{block "head" .}}{{end}}
</head>
<body>
  <div class="ui-shell">
    <aside class="ui-shell__sidebar">
      {{template "components/navigation/sidebar" .Sidebar}}
    </aside>
    <main class="ui-shell__main">
      <div class="ui-shell__content">
        {{block "content" .}}{{end}}
      </div>
    </main>
  </div>
  {{block "scripts" .}}{{end}}
</body>
</html>
{{end}}
